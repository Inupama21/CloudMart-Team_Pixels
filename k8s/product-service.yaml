# NOTE: product-service pod management has been migrated to Argo Rollouts.
# The Deployment, Service, and HPA are now defined in product-service-canary.yaml
# which uses kind: Rollout (argoproj.io/v1alpha1) for canary deployments.
#
# DO NOT add a Deployment here — it will conflict with the Rollout.
